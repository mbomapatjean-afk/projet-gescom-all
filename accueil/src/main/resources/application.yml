server:
  port: 9081
  servlet:
    context-path: /gescom

app:
  services:
    commande:
      url: ${COMMANDE_URL:http://localhost:9082/gescom/commande}
    rechercheCommande:
      url: ${RECHERCHE_COMMANDE_URL:http://localhost:9082/gescom/rechercher}

spring:
    application:
        name : accueil
    mvc:
      view:
        prefix: /WEB-INF/views/
        suffix: .jsp
    datasource:
      url: jdbc:mysql://localhost:3306/gescom_db
      username: ${DB_USERNAME:root}
      password: ${DB_PASSWORD:}
    jpa:
      show-sql: true
      properties:
        hibernate:
          format_sql: true
      hibernate:
        ddl-auto: none
    mail:
      host: smtp.gmail.com
      port: 465
      username: mbomapatjean@gmail.com
      password: mhmfdhprywcizlls
      properties:
        mail:
          smtp:
            auth: true
            ssl:
              enable: true
            socketFactory:
              port: 465
              class: javax.net.ssl.SSLSocketFactory
